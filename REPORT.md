# temp.csv 분석 프로젝트 보고서

> 작성일: 2025-10-31
> 대상 데이터: `temp.csv`
> 작성 목적: 3차례에 걸친 데이터 요약, 효율적 투자선 분석, 환경 재구성 작업 내용을 종합 정리

---

## 페이지 1 — 프로젝트 개요와 주요 산출물

### 1. 프로젝트 배경
- 삼성전자, 애플, 엔비디아 3개 종목의 일별 시세가 포함된 `temp.csv` 파일을 기반으로 분석.
- CSV는 3중 헤더 구조를 사용하여 가격 지표(`Price/High/Low/Open/Volume`)와 티커를 분리 저장.
- 분석 목표는 기본 통계 요약 → 효율적 투자선 도출 → 설정 변경에 따른 재현성 강화로 단계적으로 확장.

### 2. 주요 작업 이력
1. **1차 작업**: 데이터 구조 파악 및 기본 기술 통계 작성.
   - 종가, 거래량, 일간 수익률의 평균·표준편차·최솟값·최댓값 계산.
   - README에 표 형태로 정리하고 핵심 인사이트 도출.
   - `scripts/summarize_temp.py`를 작성하여 동일 결과를 재현할 수 있도록 함.
2. **2차 작업**: 투자 관점 분석 심화.
   - 평균-분산 최적화 기반 효율적 투자선 계산 로직 구현.
   - SVG 기반 시각화(`assets/efficient_frontier.svg`) 생성 및 README에 포함.
   - 최대 샤프 비중, 단일 자산 포트폴리오, 효율적 경계선을 비교 분석.
3. **3차 작업**: 환경 설정 변경에 대응한 파이프라인 개선.
   - 두 스크립트에 `argparse` 기반 CLI 옵션 추가(입력 경로, 연환산 일수, 위험 자유 수익률, 출력 경로 등).
   - README 업데이트로 실행 명령과 옵션 설명을 강화.
   - 최신 설정을 반영하도록 SVG를 재생성.

### 3. 산출물 요약
- **문서**: `README.md` (데이터 개요, 통계 표, 효율적 투자선 분석 및 재현 절차).
- **분석 스크립트**: `scripts/summarize_temp.py`, `scripts/efficient_frontier.py`.
- **시각 자료**: `assets/efficient_frontier.svg`.
- **재현성**: Python 표준 라이브러리만 사용하여 추가 의존성 없이 실행 가능.

---

## 페이지 2 — 데이터 요약과 분석 세부 내용

### 4. 데이터 구조 및 전처리
- CSV 헤더는 첫 행에 지표명, 두 번째 행에 티커, 세 번째 행에 데이터 유형(예: `Close`, `High`).
- 스크립트는 `csv.reader`로 세 헤더를 분리 읽고, 각 티커·지표별 컬럼 인덱스를 매핑.
- 날짜는 `datetime.strptime`로 `YYYY-MM-DD` 포맷을 파싱해 일자순 정렬.

### 5. 기본 통계 계산 절차
1. **종가 시계열 정렬**: 티커별로 `(날짜, 종가)` 리스트를 구성하고 오름차순 정렬.
2. **수익률 산출**: 인접 거래일 종가 변화율을 `%` 단위로 계산.
3. **요약 통계**: 평균, 표준편차, 최솟값, 최댓값을 공통 함수 `stats`로 계산.
4. **거래량 스케일링**: 백만 주 단위로 표현해 가독성 향상.

### 6. 통계 결과 인사이트
- **삼성전자(005930.KS)**
  - 분석 기간 내 482개 거래일 데이터 수집.
  - 종가가 기간 말 94,400원으로 최고가를 기록하며 평균 대비 +41%.
  - 거래량은 평균 1,948만 주, 최대 5,769만 주로 점진적 증가 추세.
- **애플(AAPL)**
  - 499개 거래일, 평균 종가 209.52달러, 표준편차 24.38달러로 안정적 변동성.
  - 일간 수익률은 평균 0.08%, 표준편차 1.77%로 중간 수준.
- **엔비디아(NVDA)**
  - 평균 수익률 0.33%, 표준편차 3.21%로 고수익·고변동 특성을 확인.
  - 거래량은 평균 3억 2,512만 주로 세 종목 중 압도적으로 높음.

### 7. 스크립트 활용 가이드
```bash
python scripts/summarize_temp.py --csv temp.csv
```
- `--csv` 옵션으로 대체 경로 지정 가능.
- 결과는 콘솔에 티커별 통계 요약을 출력.
- 통계 함수와 출력 형식을 README와 동일하게 유지해 문서-스크립트 간 일관성을 확보.

---

## 페이지 3 — 효율적 투자선 분석과 향후 방향

### 8. 효율적 투자선(Efficient Frontier) 구현
- **데이터 정렬**: 세 종목의 일간 수익률을 공통 거래일 기준으로 정렬.
- **연환산 설정**: `--trading-days` 기본값 252일, 필요 시 사용자 지정.
- **공분산 및 기대수익률 계산**: 표준 라이브러리만으로 평균, 공분산 행렬, 역행렬을 산출.
- **포트폴리오 최적화**: 목표 수익률 그리드에 대해 분산 최소화 가중치 계산.
- **시각화**: SVG 캔버스에 효율적 경계선, 단일 자산, 최대 샤프 포인트를 그려 README에 삽입.

### 9. CLI 확장과 재현성 강화
```bash
python scripts/efficient_frontier.py \
    --csv temp.csv \
    --output assets/efficient_frontier.svg \
    --trading-days 252 \
    --risk-free 0.0
```
- `--output`으로 SVG 저장 위치 변경 가능.
- `--risk-free`로 샤프 지수 계산 시 위험 자유 수익률을 조정.
- `--no-plot` 옵션(필요 시)을 통해 수치 요약만 출력하는 등 확장이 용이하도록 구조화.

### 10. 분석 결과 해석
- 최대 샤프 포트폴리오(위험 자유 수익률 0%)는 엔비디아 66.3%, 삼성전자 35.8%, 애플 -2.0%로 구성.
- 효율적 투자선은 고변동성 자산(엔비디아) 비중을 활용해 높은 기대수익을 도모하면서도 삼각형 자산 분산을 통해 변동성을 제어.
- 단일 자산 대비 포트폴리오 조합이 동일 위험에서 더 높은 기대수익을 제공함을 시각적으로 제시.

### 11. 향후 과제 제안
- **리스크 프리미엄 시나리오 분석**: 위험 자유 수익률을 국가별 채권 수익률로 대체하여 비교.
- **제약 조건 추가**: 공매도 금지(가중치 하한 0), 최대 비중 제한 등 현실적 제약을 고려한 최적화.
- **백테스트 확장**: 거래 비용, 리밸런싱 주기 등을 포함한 전략 성과 검증.
- **자동화 파이프라인**: CI 워크플로우에 스크립트 실행을 통합하여 데이터 갱신 시 자동 보고서 생성.

---

## 부록 — 참고 자료
- README 통계 표 및 해석
- `scripts/summarize_temp.py` 소스 코드
- `scripts/efficient_frontier.py` 소스 코드와 SVG 시각화
- `temp.csv` 원본 데이터 (사내 비공개)

